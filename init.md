优选交易平台 — 轻量级电商后端系统
一、项目定位
优选交易平台是一个为 TestPilot 智能测试 Agent 提供被测对象的电商后端服务，同时本身也可以作为独立项目写入简历。系统覆盖商品、购物车、订单、用户等核心电商模块，提供足够丰富的接口，让 Agent 能够自动生成覆盖正例、边界、异常场景的测试用例。
二、常规功能模块（必做）
1. 用户模块
- 用户注册 / 登录
- JWT 认证
- 收货地址管理
2. 商品模块
- 商品列表
- 商品详情
- 库存查询 / 库存扣减
3. 购物车模块
- 加入购物车
- 查看购物车
- 修改商品数量
- 删除购物车商品
4. 订单模块
- 创建订单
- 订单列表
- 订单状态流转（待支付、已支付、已取消、已完成）
5. 基础设施
- SpringBoot 4 + Java 21
- MyBatis + MySQL 持久化
- 统一异常处理
- 全局响应封装
  三、简历亮点（必做）
1. 统一异常处理与全局响应
- 自定义业务异常码
- 全局异常拦截器统一收敛
- 错误响应结构规范
2. JWT 认证链路
- 登录颁发 Token
- 接口拦截校验 Token
- 将用户信息落入 SecurityContext / ThreadLocal
3. 接口幂等防重复提交
- 基于 Token / 请求指纹的幂等控制
- 防止重复下单、重复支付
  四、核心亮点（推荐做）
4. 秒杀链路
- 商品库存扣减
- 基于 Redis + Lua 的原子性库存扣减，防止超卖
- 限流控制（如令牌桶 / 漏桶简化版）
5. 优惠券策略模式
- 定义多种优惠券类型（满减、折扣、直减）
- 使用策略模式隔离不同优惠券的结算逻辑
- 降低 if-else 复杂度
6. MyBatis 拦截器数据填充
- 通用字段自动填充（创建时间、更新时间、创建人、更新人）
- 主键生成逻辑下沉到持久层
  五、斟酌亮点（可选，视时间而定）
7. 接口防刷与防篡改
- 请求签名验签（基于时间戳 + 密钥 + 参数签名）
- 简单 IP 白名单 / 接口限流
- 注意：防刷容易流于表面，如果做就要能讲清楚策略和实现
8. 线程池隔离与虚拟线程异步编排
- 具体场景：下单成功后异步发送通知 / 记录埋点
- 优先使用 Java 21 虚拟线程，对资源敏感场景仍保留线程池隔离
- 注意：要能讲清楚虚拟线程与平台线程的区别、适用场景和线程池隔离的取舍
  六、不建议做的亮点
  以下技术栈对本科生 / 应届生项目而言过度复杂，容易被面试官追问到露馅：
- 订单分库分表（ShardingSphere）
- 搜索引擎 + 缓存同步（Elasticsearch）
- 多存储系统抽象（MinIO / RustFS / 七牛统一）
- 百万级 Excel 导出
- 动态 ID 生成与 WorkerId 分配
- 数据源治理 + ShardingSphere 共存
  七、项目与 TestPilot 的关系
  优选交易平台为 TestPilot 提供真实的被测接口。TestPilot 的 Agent 可以读取接口文档，自动生成覆盖以下场景的测试用例：
- 登录成功 / 失败
- 商品查询
- 购物车操作
- 下单成功 / 库存不足 / 未授权
- 秒杀场景
- 优惠券结算
  最终形成 "优选交易平台 + TestPilot 智能测试 Agent" 的完整技术故事。