<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mallu.payment.mapper.PaymentFlowMapper">
    <resultMap id="BaseResultMap" type="com.example.mallu.payment.entity.PaymentFlow">
        <id column="id" property="id"/>
        <result column="trade_no" property="tradeNo"/>
        <result column="order_no" property="orderNo"/>
        <result column="order_id" property="orderId"/>
        <result column="pay_amount" property="payAmount"/>
        <result column="result" property="result"/>
    </resultMap>
    <select id="selectByTradeNo" resultMap="BaseResultMap">
        SELECT id, trade_no, order_no, order_id, pay_amount, result
        FROM payment_flow WHERE trade_no = #{tradeNo} LIMIT 1
    </select>
    <insert id="insertIgnore">
        INSERT IGNORE INTO payment_flow (trade_no, order_no, order_id, pay_amount, result)
        VALUES (#{tradeNo}, #{orderNo}, #{orderId}, #{payAmount}, #{result})
    </insert>
</mapper>
